/**
 * Interface component for CropShapeInterface
 * 
 * Carries the indexes of lower and upper bounds of a cropped image over an axis 
 */
export default interface CropShapeInterface{
    lower: number;
    upper: number;
}